import React from 'react'
import { BrowserRouter, Routes } from 'react-router-dom'

export default function App() {
    return (
        <React.StrictMode>
            <BrowserRouter>
                <Routes>
                </Routes>
            </BrowserRouter>
        </React.StrictMode>
    )
}